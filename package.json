{
  "name": "viberoom",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run --workspace=apps/web dev",
    "build": "npm run --workspace=apps/web build",
    "start": "npm run --workspace=apps/web start"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=22.2.0"
  }
}
